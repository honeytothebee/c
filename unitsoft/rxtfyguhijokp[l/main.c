#include <stdio.h>
#include <stdlib.h>

int main()

{
    int a,b;
    scanf("%d %d", &a, &b);
    if(b<0)
        printf("%d %d", a+b );


}

























    /* int a;
     scanf("%d", &a);
     if(a>0)
        printf("%d는 양의 값입니다", a);

        else if(a<0)
            printf("%d는 음의 값입니다.", a);
     return 0;
 */






